'use strict'

/**
 * 仅作 JSDoc 引用，无运行时导出。
 *
 * @typedef {object} WinSession
 * @property {import('@syllm/brickly-sdk').WindowHandle} handle
 * @property {'control'|'scenario'} role
 * @property {string} [scenario]
 * @property {string} title
 * @property {number} createdAt
 * @property {Array<{ at: number, event: string, detail?: unknown }>} events
 */

module.exports = {}
