/**
 * com.brickly.log-searcher — Preload
 *
 * 业务在 Go runtime（gRPC invoke / interact）。底座通过 window.brickly 注入
 * invoke、interact、call 与窗口控制。此处仅作预留扩展点。
 */
console.info('[com.brickly.log-searcher][preload] loaded')
