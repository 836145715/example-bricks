# C++ SDK Lab

C++ SDK Lab generated by create-brickly.

create-brickly 目前没有 C++ runtime，本目录的 `ui/`、`manifest.json`、命令与 `--runtime go --ui h5 --preset window` 一致；runtime 用手写的 `runtime/cpp`（`brickly-sdk-cpp` + nlohmann/json）。

Generated by create-brickly.

## 运行方式

Runtime 使用 C++ SDK，入口由 manifest.runtime.entry 声明。

体验窗先 `start()`。开独立窗的命令须声明 `window: standalone`。后台定时开窗请 `invoke` 一条 `window: standalone` 的命令，不要在定时器里直接 `createWindow`。runtime 对每扇窗 `expose` 方法表，子页用 `request('xxx')` / `on('tick')`。`expose` 跟窗走，不跟开窗那次 command 走。关 attached 窗即收这次 Call；关 standalone 窗且没有 Handle / Call 即收 runtime。

JSON 进出在 SDK 里仍是字符串。本示例用 [nlohmann/json](https://github.com/nlohmann/json) 3.11.3（`runtime/cpp/nlohmann/json.hpp`）编解码。

C++ SDK 还没有独立发版。构建必须能找到旁边的 `ai-bricks`（或 `BRICKLY_HOME`），并安装 Go + gcc/g++。

## 构建

在本目录：

```bash
npm run setup -- --local
```

或在仓库根：

```bash
node scripts/setup-brick.cjs --local com.brickly.cpp-sdk-lab
```

当前平台会得到 `runtime/<platform>/brick.exe`（或 `brick`）以及同目录的 `brickly.dll` / `libbrickly.dylib` / `libbrickly.so`。不要交叉编译：c-shared 绑定了本机 C 工具链。Windows 构建会加上 `-ldflags="-s -w"`，否则 Go 1.25 打出的 DLL 无法加载。macOS 会把 dylib 的 install name 写成 `@rpath/libbrickly.dylib`，并给入口加上 `@loader_path` rpath；宿主 cwd 是 Brick 根目录，裸文件名解析不到 sidecar。

## UI 开发

原生 H5 模板直接生成到 `ui/`，manifest 中的入口是 `ui/index.html`，不需要构建步骤。

## 示例能力

- `timer`：打开跟随 runtime 的倒计时窗。会话结束或 runtime 停了，窗就关。参数：`seconds`（秒）: number；示例输入：`{"seconds":60}`
- `pin`：打开常驻倒计时窗。窗不关，runtime 不关。参数：`seconds`（秒）: number；示例输入：`{"seconds":60}`

## 依赖工具

未选择其它工具。

脚手架默认生成 `dependencies[brickId].commands = ["*"]`，表示允许调用目标工具的全部可见命令。手写 manifest 时不要省略 `commands`，省略或空数组只展示依赖关系，不授权命令调用。
